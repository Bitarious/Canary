Continue only the development task explicitly authorized in the current session. Read AGENTS.md, docs/handoff.md, and docs/implementation-plan.md. Inspect the current source and tests before editing. Do not start a historical backlog item or a training run merely because an old document describes it.

Work alone. Preserve user changes, released results, model hashes, and data boundaries. Entire is no longer used. Follow normal sandbox, authentication, and approval controls.

Before any authorized training run, explain and record its objective, data splits, updated and frozen parameters, hardware/cost limit, stopping rules, evaluation, and outputs. Verify current funds and a provider stop guard before paid compute. Never reuse a released test run for fitting.

Run relevant verification and update the handoff. Commit, push, or publish only within the current user's authorization. Report completed work and any unresolved dependency. Do not loop indefinitely on a permanent failure.
