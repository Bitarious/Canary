# Compatibility entry point for pip. Python 3.12 or newer.
# Use `uv sync --locked --extra training --extra model-access` for exact versions.
# CPU-only pip installation: add --extra-index-url https://download.pytorch.org/whl/cpu
-e .[training,model-access]
-r driftops3d/requirements.txt
